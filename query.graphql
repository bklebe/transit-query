{
    Vehicle {
        label @filter(op: "regex", value: ["$newRedLineCars"]) 
              @output
        
        trip {
            id @output
            route_id @filter(op: "has_substring", value: ["$redLineRouteId"]) @output
        }
    }
}