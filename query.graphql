{
  Vehicle {
    label @output @filter(op: "regex", value: ["$newGreenLineCars"])
    latitude @output
    longitude @output

    trip {
      id @output
      route_id @output @filter(op: "regex", value: ["$routeId"])
    }
  }
}
